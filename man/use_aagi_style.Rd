% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_aagi_style.R
\name{use_aagi_style}
\alias{use_aagi_style}
\title{Apply AAGI theme and default scales to a ggplot2 object}
\usage{
use_aagi_style(
  palette = NULL,
  discrete = TRUE,
  reverse = FALSE,
  colours = NULL,
  ...
)
}
\arguments{
\item{palette}{Character name of a palette supported by
\code{\link[AAGIPalettes:aagi_palettes]{AAGIPalettes::aagi_palettes()}}. If \code{NULL} and \code{discrete = TRUE}, use the
official colours in \link[AAGIPalettes:aagi_colours]{AAGIPalettes::aagi_colours}.}

\item{discrete}{Logical; if \code{TRUE}, use discrete colour/fill scales.
Defaults to \code{TRUE}.}

\item{reverse}{Logical; if \code{TRUE}, reverse the palette order.}

\item{colours}{Character vector of official \acronym{AAGI} colour names used
for continuous interpolation. Ignored for default discrete scales unless
\code{discrete = FALSE}.}

\item{...}{Arguments passed to \code{\link[=theme_aagi]{theme_aagi()}}.}
}
\value{
A list of \CRANpkg{ggplot} components that can be added with \code{+}.
}
\description{
Convenience wrapper that applies \code{\link[=theme_aagi]{theme_aagi()}}, \code{\link[=scale_colour_aagi]{scale_colour_aagi()}} and
\code{\link[=scale_fill_aagi]{scale_fill_aagi()}} together.
}
\examples{
library(ggplot2)

# Default AAGI theme + official AAGI discrete colours
ggplot(mtcars, aes(wt, mpg, colour = factor(cyl), fill = factor(cyl))) +
  geom_point(shape = 21, size = 3) +
  use_aagi_style()

# Use a named palette instead
ggplot(mtcars, aes(wt, mpg, colour = factor(cyl), fill = factor(cyl))) +
  geom_point(shape = 21, size = 3) +
  use_aagi_style(palette = "aagi_RdTl")

# Continuous colours
ggplot(mtcars, aes(wt, mpg, colour = hp)) +
  geom_point(size = 3) +
  use_aagi_style(discrete = FALSE)

}
