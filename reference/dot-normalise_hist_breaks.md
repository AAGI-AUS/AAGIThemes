# Normalise the `breaks` argument for hist_aagi()

Normalise the `breaks` argument for hist_aagi()

## Usage

``` r
.normalise_hist_breaks(breaks)
```

## Arguments

- breaks:

  User-supplied breaks selector.

## Value

One of "pretty", "scott", or "exact".
